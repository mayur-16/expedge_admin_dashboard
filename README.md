# expedge_admin_dashboard

A new Flutter project.

# how to run 

flutter run -d web-server --web-port=5000
# Then open http://localhost:5000 in Safari
# Use Safari Web Inspector + Flutter DevTools (from terminal link)


# building web build
flutter build web

# hosting locally
cd /path/to/build/web

# Node.js (if you have it)
npx http-server -p 8080

